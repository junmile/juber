type RegistrationCheckResponse {
  ok: Boolean!
  error: String
  user: User
}

type Query {
  RegistrationCheck(
    type: String!
    phoneNumber: String
    email: String
  ): RegistrationCheckResponse!
}
